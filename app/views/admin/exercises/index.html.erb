
<ul>
<% @exercises.each do |exercise| %>
  <li><%= exercise.name %></li>
  <%= button_to "Approve", admin_exercise_path(id: exercise.id), method: :patch %>
  <%= button_to "Edit", edit_admin_exercise_path(exercise), method: :get %>
<% end %>
</ul>
